---
phase: 9
plan: 1
wave: 1
---

# Plan 9.1: OptionEAE Ingestion Schema & Parsers

## Objective
Update the Pydantic schemas and parsing logic to extract and validate `OptionEAE` events from both Flex XML and CSV sources, preparing them for the FIFO engine.

## Context
- .gsd/SPEC.md
- .gsd/phases/9/RESEARCH.md
- src/ibkr_tax/schemas/ibkr.py
- src/ibkr_tax/services/flex_parser.py
- src/ibkr_tax/services/csv_parser.py

## Tasks

<task type="auto">
  <name>Create OptionEAE Pydantic Schema</name>
  <files>src/ibkr_tax/schemas/ibkr.py</files>
  <action>
    - Add an `OptionEAECreate` schema inheriting from `BaseModel`.
    - Fields: account_id (str), currency (str), fx_rate_to_base (Decimal), symbol (str), underlying_symbol (str), strike (Decimal), expiry (date), put_call (str), date (date), transaction_type (str), quantity (Decimal), trade_price (Decimal), etc.
    - Ensure strict data types (`Decimal` for all amounts).
  </action>
  <verify>pytest -v tests/test_schemas.py (if existing) or simply run python -m py_compile src/ibkr_tax/schemas/ibkr.py</verify>
  <done>OptionEAECreate Pydantic model is defined and passes type checks.</done>
</task>

<task type="auto">
  <name>Extract OptionEAE in Parsers</name>
  <files>
    src/ibkr_tax/services/flex_parser.py
    src/ibkr_tax/services/csv_parser.py
  </files>
  <action>
    - In `flex_parser.py`, map `ibflex.OptionEAE` events to the new `OptionEAECreate` schema. Return these alongside `trades` and `cash_transactions` or process them directly.
    - In `csv_parser.py`, add support for parsing the "OptionEAE" section if it exists, mapping it to `OptionEAECreate`.
    - Ensure robust error handling for missing sections or fields.
  </action>
  <verify>pytest -v tests/test_parsers.py</verify>
  <done>Both XML and CSV parsers can extract OptionEAE data without crashing and return typed objects.</done>
</task>

## Success Criteria
- [ ] `OptionEAECreate` schema correctly validates raw data.
- [ ] `flex_parser` successfully extracts Expiration/Assignment/Exercise records.
- [ ] `csv_parser` supports the OptionEAE CSV section.
