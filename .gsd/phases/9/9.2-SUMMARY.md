# Plan 9.2 Summary: Option Engine & FIFO Integration

## Accomplishments
- Implemented `OptionEngine` in `src/ibkr_tax/services/option_engine.py`.
- Modified `database.py` to allow nullable `sell_trade_id` in `Gain` model (for expirations).
- Integrated `OptionEngine` into `pipeline.py`:
  - `OptionEAE` data is processed *before* trades are imported.
  - Option lots are closed.
  - Underlying stock trades are adjusted for premiums.
- FIFO Engine remains cleaner as it only sees adjusted "base" stock trades.

## Verification
- Modified files compile correctly.
- Pipeline structure allows optional processing of `OptionEAE` data.
