---
phase: 9
plan: 3
wave: 3
---

# Plan 9.3: Options Edge Cases Testing

## Objective
Write comprehensive unit and integration tests to verify the correctness of option expirations, assignments, and exercises modifying stock basic costs and generating proper tax entries.

## Context
- .gsd/SPEC.md
- .gsd/phases/9/RESEARCH.md
- tests/test_parsers.py
- tests/test_fifo.py
- tests/test_tax_aggregator.py

## Tasks

<task type="auto">
  <name>Test Option Expirations</name>
  <files>tests/test_fifo.py</files>
  <action>
    - Simulate an `OptionEAE` event with `transaction_type="Expiration"`.
    - Verify that the corresponding option `FIFOLot` is closed.
    - Assert that a realized Loss/Gain is generated in the `Termingeschäfte` pool matching the option premium.
  </action>
  <verify>pytest -v tests/test_fifo.py -k "expiration"</verify>
  <done>Option expiration properly realizes PnL in Termingeschäfte without affecting any underlying stock trades.</done>
</task>

<task type="auto">
  <name>Test Option Assignments and Exercises</name>
  <files>tests/test_fifo.py</files>
  <action>
    - Simulate Long Call Exercise, Short Call Assignment, Long Put Exercise, and Short Put Assignment.
    - Provide mock stock `Trade` entries that occur simultaneously.
    - Assert that the option `FIFOLot` is closed without immediate realized PnL.
    - Assert that the underlying stock's `cost_basis_total` or `proceeds` is correctly adjusted by the option's value.
  </action>
  <verify>pytest -v tests/test_fifo.py -k "exercise or assignment"</verify>
  <done>Option premiums correctly modify stock cost bases/proceeds across all 4 exercise/assignment scenarios.</done>
</task>

## Success Criteria
- [ ] 100% test coverage for all 5 Option Action scenarios (Expiration + 4 Exercise/Assignment types).
- [ ] No regression on existing FIFO and standard taxation logic.
