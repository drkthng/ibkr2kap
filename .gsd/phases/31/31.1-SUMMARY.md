# Plan 31.1 Summary

- Updated `ManualPosition` model in `src/ibkr_tax/models/database.py` with 9 new fields to mirror `Trade` records.
- Updated `add_manual_position` in `src/ibkr_tax/db/repository.py` to support these new fields.
- Database auto-migrates on next app startup.
