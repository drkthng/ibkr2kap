# Plan 7.1 Summary: FIFO Engine Core Logic

## What was done
- Implemented `FIFOEngine` in `src/ibkr_tax/services/fifo.py`.
- Added logic for:
    - Creating `FIFOLot` from `BUY` trades with EUR-converted cost basis.
    - Matching `SELL` trades against open `FIFOLots` (FIFO order).
    - Splitting lots for partial fills.
    - Calculating `Gain` (realized PnL) in EUR.
    - Handling fractional shares and precise `Decimal` math.
- Updated `Trade` model to make `open_close_indicator` nullable to accommodate all trade sources.
- Created `tests/test_fifo.py` with comprehensive unit tests for buy/sell matching, multiple lots, and fractional quantities.

## Verification Results
- `tests/test_fifo.py`: 3 tests passed.
- Logic verified for EUR conversion and proportional cost basis allocation.
