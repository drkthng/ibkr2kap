---
phase: 14
plan: 3
wave: 3
depends_on: [2]
files_modified: ["src/ibkr_tax/services/pipeline.py", "src/ibkr_tax/services/excel_export.py", "tests/services/test_pipeline.py", "tests/services/test_excel_export.py"]
autonomous: true

must_haves:
  truths:
    - "Pipeline processes FX FIFO after asset FIFO"
    - "Excel report includes a section for Section 23 EStG FX Gains"
  artifacts:
    - "excel_export.py updated"
    - "pipeline.py updated"
---

# Plan 14.3: Integration and Export

<objective>
Integrate the FX FIFO engine into the main data runner pipeline and expose the results in the Excel export.

Purpose: Ensures the engine runs on imported data and the user receives the § 23 EStG outputs.
Output: Updated pipeline and Excel reporter.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/ibkr_tax/services/pipeline.py
- src/ibkr_tax/services/excel_export.py
</context>

<tasks>

<task type="auto">
  <name>Integrate FX Data Pipeline</name>
  <files>src/ibkr_tax/services/pipeline.py</files>
  <action>
    Update pipeline runner (e.g. `process_all` or similar integration method) to run the `FXFIFOEngine` after the regular `FIFOEngine` runs.
    AVOID: Double counting if run multiple times. Ensure idempotency (e.g., delete old FXLots/FXGains before running).
  </action>
  <verify>pytest</verify>
  <done>Pipeline executes FX FIFO correctly.</done>
</task>

<task type="auto">
  <name>Export FX Gains to Excel</name>
  <files>src/ibkr_tax/services/excel_export.py</files>
  <action>
    Add a new sheet "Währungsgewinne (Anlage SO)" to the Excel export.
    It should query taxable `FXGain`s (`is_taxable_section_23` == True) and sum the `realized_pnl` by year. 
    It should also display a detailed list of these taxable gains (Currency, Acquisition Date, Disposal Date, Disposed Amount, Gain in EUR).
    AVOID: Changing the Anlage KAP columns. Section 23 is a different pool.
  </action>
  <verify>pytest tests/services/test_excel_export.py</verify>
  <done>Excel produces a sheet for section 23 gains.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Pipeline runs fully
- [ ] Excel export contains the new sheet and logic
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
