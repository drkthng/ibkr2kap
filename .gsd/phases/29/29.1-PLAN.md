---
phase: 29
plan: 1
wave: 1
---

# Plan 29.1: FX Engine Backend Redesign

## Objective
Redesign the core `FXFIFOEngine` to only track explicit FX conversion trades (`asset_category == 'CASH'`). Update the database model to remove unused relations and rewrite all FX engine tests.

## Context
- `C:\Users\Gordon\.gemini\antigravity\brain\6b69fe4e-275b-40db-bad9-07c6e5d7078b\fx_warnings_analysis.md` (Research on German tax law for FX)
- `d:\Antigravity\IBKR2KAP\src\ibkr_tax\services\fx_fifo_engine.py`
- `d:\Antigravity\IBKR2KAP\src\ibkr_tax\models\database.py`
- `d:\Antigravity\IBKR2KAP\tests\services\test_fx_fifo_engine.py`

## Tasks

<task type="auto">
  <name>Update Database Model</name>
  <files>d:\Antigravity\IBKR2KAP\src\ibkr_tax\models\database.py</files>
  <action>
    - Remove the `cash_transaction_id` foreign key from the `FXFIFOLot` model.
    - We only need `trade_id` because only CASH trades create FX lots now.
  </action>
  <verify>pytest -q tests/test_fx_models.py</verify>
  <done>FXFIFOLot model no longer has cash_transaction_id relation</done>
</task>

<task type="auto">
  <name>Rewrite FXFIFOEngine Logic</name>
  <files>d:\Antigravity\IBKR2KAP\src\ibkr_tax\services\fx_fifo_engine.py</files>
  <action>
    - Rewrite `_get_fx_events(account_id)` to ONLY query `Trade` where `asset_category == 'CASH'` (and currency != 'EUR').
    - Completely remove querying of `CashTransaction`.
    - Modify the logic to process CASH trades: a SELL of EUR.USD means buying USD (acquisition amount = proceeds). A BUY of EUR.USD means selling USD (disposal amount = proceeds).
    - If a cross-currency trade happens (e.g. BUY GBP.USD), it yields two events: +GBP and -USD.
    - Remove the bidirectional matching logic. Use simple FIFO: match disposals against the running pool of acquisitions.
  </action>
  <verify>pytest -q tests/services/test_fx_fifo_engine.py</verify>
  <done>Engine processes only CASH trades and uses standard FIFO</done>
</task>

<task type="auto">
  <name>Rewrite FX Tests</name>
  <files>d:\Antigravity\IBKR2KAP\tests\services\test_fx_fifo_engine.py</files>
  <action>
    - Update `test_fx_taxable_gain_within_year` and `test_fx_non_taxable_gain_after_one_year` to use CASH trades (EUR.USD) instead of stock trades / cash txs.
    - Update `test_multiple_currencies` to use EUR.USD and EUR.GBP CASH trades.
    - Add test `test_dividends_not_tracked` (assert no FX lot created).
    - Add test `test_stock_trades_not_tracked` (assert no FX lot created).
  </action>
  <verify>pytest -q tests/services/test_fx_fifo_engine.py</verify>
  <done>All 5 tests pass focusing on CASH trades only</done>
</task>

## Success Criteria
- [ ] `FXFIFOLot` lacks `cash_transaction_id`.
- [ ] `FXFIFOEngine` solely tracks `CASH` trades.
- [ ] All FX tests run and pass.
